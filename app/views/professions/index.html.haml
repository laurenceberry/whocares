.intro
  %h1 Professions

%ul.professions
  - @professions.each do |profession|
    %a{:href => profession_roles_path(profession)}
      %li.profession
        .head
          %h3=profession.name
        .description
          =truncate(profession.description, length: 140)
