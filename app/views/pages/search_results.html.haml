.intro
  - if @roles.present?
    %h1 Results for "#{params[:search]}"


- if @roles.present?

  %ul.roles
    - @roles.each do |role|
      %a{:href => profession_role_path(role.profession_id, role)}
        %li.role
          .head
            %h3=role.name
          .description
            -if role.description.blank?
              %p
                Improve the information about this role
            -else
              %p=truncate(role.description, length: 140)

          %ul.jobs-card
            - role.jobs.each do |job|
              %li.job-tag
                = job.title
- else
  %p
    There are no roles containing #{params[:search]}.
